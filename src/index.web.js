import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('UniversalBoilerplate', () => App);
AppRegistry.runApplication('UniversalBoilerplate', { rootTag: document.getElementById('root') });